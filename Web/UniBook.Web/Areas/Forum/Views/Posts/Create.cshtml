@model PostViewModel

<div class="container text-white">
    <div class="row">
        <div class="col-md-8 col-md-offset-2">
            <h1 class="text-white">Задаване на въпрос</h1>

            <script src="/lib/tinymce/tinymce.min.js" type="text/javascript"></script>
            <script type="text/javascript">
                tinyMCE.init({
                selector: "textarea",
                plugins: [
                    "image paste table link code media"
                ]
            });</script>

            <form method="post" asp-area="Forum">
                <div>
                    <label asp-for="@Model.Title">Заглавие</label>
                    <input asp-for="@Model.Title" class="form-control" />
                    <span asp-validation-for="@Model.Title" class="text-danger"></span>
                </div>

                <div>
                    <label asp-for="@Model.Content">Съдържание</label>
                    <textarea asp-for="@Model.Content" class="form-control"></textarea>
                    <span asp-validation-for="@Model.Content" class="text-danger"></span>
                </div>

                <div>
                    <label asp-for="@Model.Category">Категория</label>
                    <select asp-for="@Model.Category" class="form-control">
                        <option value="general">Общи</option>
                        <option value="books">Книги</option>
                        <option value="news">Новини</option>
                        <option value="suggestions and problems">Предложения и проблеми</option>
                    </select>
                    <span asp-validation-for="@Model.Category" class="text-danger"></span>
                </div>
                <hr />
                <input type="submit" class="btn btn-primary" value="Изпрати" />
            </form>
        </div>
    </div>
</div>